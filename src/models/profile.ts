export interface Profile {
  eneagram: {
    name: string;
    code: string;
    description: string;
  };
  disc: {
    name: string;
    code: string;
    description: string;
  };
  personality16: {
    name: string;
    code: string;
    description: string;
  };
  temperaments: {
    name: string;
    code: string;
    description: string;
  };
}
